<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <Button
        android:id="@+id/start_download"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="46dp"
        android:onClick="onClick"
        android:text="@string/start_download" />

    <Button
        android:id="@+id/stop_download"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/start_download"
        android:layout_marginTop="24dp"
        android:onClick="onClick"
        android:text="@string/stop_download" />

    <Button
        android:id="@+id/delete_download_file"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/stop_download"
        android:layout_below="@+id/stop_download"
        android:layout_marginTop="25dp"
        android:onClick="onClick"
        android:text="@string/delete_apk_file" />

    <TextView
        android:id="@+id/tv_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/delete_download_file"
        android:layout_marginTop="20dp" />

</RelativeLayout>
